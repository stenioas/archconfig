{
  "commands": [
    "sudo systemctl enable libvirtd", // Launch Virtual Machine Manager
    "sudo usermod -aG libvirt ${USER}"
  ],
  "packages": [
    "qemu-desktop", // QEMU with desktop support
    "libvirt", // Virtualization API
    "virt-manager", // Virtual Machine Manager GUI
    "dnsmasq", // Network infrastructure for virtual machines
    "ebtables" // Ethernet bridge frame table administration tool
  ]
}
