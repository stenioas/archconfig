{
  "commands": [
    "[[ -d \"${HOME}/.dotfiles\" ]] && rm -rf \"${HOME}/.dotfiles\" && git clone https://github.com/stenioas/dotfiles.git ${HOME}/.dotfiles && bash ${HOME}/.dotfiles/install_dotfiles.sh" // Clone and install personal dotfiles
  ],
  "packages": []
}
